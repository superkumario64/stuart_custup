<?php

/**
 * Implementing hook_theme().
 */
function cd_integration_theme($existing, $type, $theme, $path) {

    return array(
      'cd_integration_theme' => array(
        'variables' => array('items' => array(), 'title' => ''),
      )
    );
  }